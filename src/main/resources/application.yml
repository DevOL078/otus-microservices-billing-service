spring:
  application.name: billing-service
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/billing-service
    username: postgres
    password: password
